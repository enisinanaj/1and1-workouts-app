platform :ios, "9.0"
use_frameworks!

target 'microtime.tracker' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods
  pod 'SQLite.swift', '~> 0.11.0'
end
